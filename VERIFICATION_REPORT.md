# Verification Report

Scope: all 28 checks in `MANUAL_CHECKLIST.md` (MC-001…MC-028). This repo's own
`.workflow/state` is `EXECUTE_CHECKLIST` for a real in-flight run against a
real issue (`unclehq/stagegate#2`) — this verification pass never invoked the
real `from-issue.sh`/`change-workflow.sh` against that live state, never ran a
real `gh issue close`, and never spent real agent-pipeline budget. All live
exercises below used disposable `mktemp -d` scratch copies of the scripts,
with `change-workflow.sh` and/or `gh` stubbed at the exact minimal point
needed to keep the exercise safe (see Evidence). Checks whose "Expected
result" requires a real live agent pipeline reaching `COMPLETE` organically,
a real GitHub issue actually closed, concurrent live clones, or killing a
live unrelated process are marked NOT RUN / BLOCKED — no fabricated result is
reported for them, per instruction.

## Results

Check ID | Action performed | Expected result | Actual result | Evidence | Status | Defect
---|---|---|---|---|---|---
MC-001 | Not executed with a real live agent pipeline (would spend real budget and could collide with this checkout's own live run for issue #2). Confirm/decline mechanics and seed→prompt→run ordering exercised instead via scratch-copy CLI runs (see MC-002). | Seed before prompt, driver starts in-process on confirm, `Run:` hint absent on success | Not observed end-to-end with the real driver | — | NOT RUN | —
MC-002 | Ran real `from-issue.sh --change` (unmodified script, real `gh` fetch of `unclehq/stagegate#2`) in a scratch dir, with `change-workflow.sh` stubbed to exit 99 and print to stderr if invoked, for "wrong word" and "EOF" inputs | Each treated as decline, exit 0, no driver invocation, no `.workflow/` created, `Run:` hint printed | Both cases: exit 0, "Not confirmed... nothing else ran", `Run: ./scripts/change-workflow.sh` printed, `.workflow` absent, driver stub never printed its stderr line | `out1.txt`/`out2.txt` transcripts (session output above) | PASS | —
MC-003 | Same scratch setup, piped (non-TTY) stdin supplying `RUN\n` immediately | Prompt appears on the pipe, exact word proceeds to the driver exactly as a TTY would; no `[ -t 0 ]` bypass | Prompt text printed ("Type RUN exactly..."), driver stub invoked (its stderr line appeared), no early decline from lack of TTY | `out3.txt` transcript above | PASS (blocking behavior with input already queued; EOF-declines-on-pipe covered by MC-002's EOF case which was also run over a pipe) | —
MC-004 | Not executed with a real live agent pipeline or a real `gh issue close`. Close-decision logic and the exact `gh issue close` invocation exercised instead: real `from-issue.sh --change` against real issue `unclehq/stagegate#2`, `change-workflow.sh` stubbed to write `FINAL_AUDIT.md`=`READY` + a correct `.workflow/audit-verdict`, real `gh` on PATH with only `issue close` intercepted (all other subcommands, including the real fetch, forwarded to the real `gh` binary) | Verdict artifact holds current run id/class/hash; origin matches; issue closes with a comment naming the verdict | Stub-`gh` log recorded exactly: `issue close 2 --repo unclehq/stagegate --comment Closed by stagegate: change workflow completed with FINAL_AUDIT.md verdict `READY`. See FINAL_AUDIT.md and .workflow/change.diff...`; output printed `Closed unclehq/stagegate#2 (verdict: READY).`; `.workflow/audit-verdict` held `<run-id>\tREADY\t<sha256>` | session transcript above ("MC-004 mechanics") | NOT RUN (full check, live pipeline + real close) — close-decision logic and `gh` argument construction independently confirmed correct | —
MC-005 | Same harness, `change-workflow.sh` stub set to write `FINAL_AUDIT.md`=`READY WITH NON-BLOCKING ISSUES` | Stored class `READY_WITH_NON_BLOCKING_ISSUES`; issue closes | Output: `Closed unclehq/stagegate#2 (verdict: READY_WITH_NON_BLOCKING_ISSUES).`; stub-gh log shows the close call with that class in the comment | session transcript above ("MC-005 mechanics") | NOT RUN (full check, live pipeline + real close) — boundary class independently confirmed to authorize closing | —
MC-006 | Hermetic `close-flow-test.sh` (99 assertions, re-run this session, exit 0) drives every negative branch (`NOT_READY`, `UNKNOWN`, missing verdict, malformed verdict, run-id mismatch, origin mismatch, audit-hash mismatch) via the real `from-issue.sh` functions sourced in scratch repos with a stubbed `gh`/driver. Additionally re-confirmed the `NOT READY` branch with the real CLI + real fetch (see MC-028 row) | Every case leaves the issue open, no close/comment call, reason printed, `NOT_READY`/`UNKNOWN` distinguished | `close-flow-test.sh: 99 checks passed`, exit 0; live re-check: "Final audit verdict: NOT_READY — leaving unclehq/stagegate#2 open.", stub-gh close log absent | test run output above; "MC-028/MC-006 mechanics" transcript | PASS | —
MC-007 | Ran real `from-issue.sh --change` with driver stub exiting 99 after confirmation (see MC-002 setup); `close-flow-test.sh` additionally covers exit 1/7/130 hermetically | Outer process reports driver failure plainly and returns the exact driver status; no close attempted | Live: exit code 99 returned unchanged, "change-workflow.sh exited 99; unclehq/stagegate#2 was not closed." printed, driver-stub's own stderr line present proving it ran; hermetic: `driver-exit-1`/`-7`/`-130` cases all passed inside the 99-check run | "non-TTY piped, confirm with RUN" transcript above; close-flow-test.sh pass count | PASS | —
MC-008 | Hermetic `close-flow-test.sh` cases `curl-fallback-skips-close`, `gh-unauthenticated-skips-close`, `gh-missing-skips-close` (all included in the 99 passing checks, re-run this session). Real `gh auth logout` against the operator's actual authenticated session was not performed — that would disrupt real credentials outside this check's scope, matching `CHANGE_TEST_REPORT.md`'s own choice to stub rather than log out | No close attempt; issue stays open; explicit skip reason; overall exit 0 | All three cases passed in the 99-check run (see MC-023 evidence) | close-flow-test.sh pass output | PASS (stubbed `gh`/PATH cases); NOT RUN (real `gh auth logout` / real unauthenticated variant) | —
MC-009 | Hermetic `close-flow-test.sh` case `gh-close-fails` (`FAKE_GH_CLOSE_RC=1`), included in the 99 passing checks | Non-zero exit, error printed, states workflow completed and only closure failed, issue remains open | Case asserts exit 1, `"gh issue close failed for owner/repo#42."`, `"only the close failed"`, and that a close attempt (which failed) was logged | close-flow-test.sh pass output | PASS (stubbed); NOT RUN (real `gh issue close` failure against a live disposable issue) | —
MC-010 | Ran real `from-issue.sh --change` against real issue `unclehq/stagegate#2` in a scratch dir pre-seeded with `.workflow/state=IMPLEMENT` and `.workflow/origin=unclehq/stagegate\t999` (a different issue) | Refuses before seeding/prompting/dispatch/GitHub access; names the conflicting identity; preserved files untouched | Exit 1; `"Refusing to seed unclehq/stagegate#2: this checkout has an in-flight change workflow (state: IMPLEMENT) that does not belong to it."` naming origin owner `999`; `.workflow/state`+`.workflow/origin` byte-identical before/after (hash-compared); `CHANGE_REQUEST.md` not written | "MC-010" transcript above | PASS (the `from-issue.sh`-side seed gate). The mirror-image `change-workflow.sh` preflight refusal (invoked directly with mismatched `STAGEGATE_ORIGIN_*`) is covered only by the hermetic `preflight-origin-mismatch`/`preflight-origin-absent` cases in `close-flow-test.sh` (also in the 99 passing checks), not independently re-run live in this pass | —
MC-011 | Not executed — requires a real agent pipeline reaching an internal `human_gate` and declining it mid-run, then resuming; no live pipeline was run this pass | Fetch/seed step does not overwrite `CHANGE_REQUEST.md` on same-origin resume; driver resumes from saved state; eventual closure targets the same issue once | Not observed with a real pipeline. Partial coverage: hermetic `seed-gate-same-origin-resumes` case (in the 99 passing checks) confirms the seed-skip decision logic in isolation | close-flow-test.sh pass output (partial) | NOT RUN | —
MC-012 | Hermetic `close-flow-test.sh` case `lock-held-by-live-pid` uses the *real* `change-workflow.sh` (copied unmodified into the scratch repo) against a real live PID (the test's own `$$`), included in the 99 passing checks | Second invocation refuses promptly, names the live PID, touches no data; lock persists while held | Case asserts exit 1, message names pid `$$`, and that `.workflow/lock/pid` still exists after the refusal | close-flow-test.sh pass output | PASS (uses the real driver script and a real live PID, not a stub) | —
MC-013 | Hermetic `close-flow-test.sh` case `lock-stale-pid-cleared` uses the real `change-workflow.sh` against a real, verified-dead PID (a backgrounded subshell `wait`ed on before use), included in the 99 passing checks | Stale lock identified and cleared, driver proceeds, lock removed on exit | Case asserts "Clearing stale lock" printed, driver reached `COMPLETE`, and `.workflow/lock` directory absent afterward | close-flow-test.sh pass output | PASS | —
MC-014 | Hermetic `close-flow-test.sh` case `stale-audit-rejected`: real `change-workflow.sh`, a precreated stale `READY` `FINAL_AUDIT.md`, reviewer stubbed as `/usr/bin/true` (exits 0, writes nothing), included in the 99 passing checks | Stale audit removed before reviewer runs; absence of new output is a hard failure before `COMPLETE`; no verdict artifact written | Case asserts exit 1, `"Required file missing or empty: FINAL_AUDIT.md"`, no `.workflow/audit-verdict` created, state unchanged at `FINAL_AUDIT` | close-flow-test.sh pass output | PASS | —
MC-015 | Re-ran `./scripts/tests/audit-verdict-test.sh` this session (26 checks, exit 0) covering all listed formats: `READY`, `READY WITH NON-BLOCKING ISSUES`, `NOT READY`, `## READY`, emphasized text, trailing blanks, CRLF, `Rerun until READY`, concatenated phrases, empty file, trailing prose | Exact phrases classify correctly after permitted cleanup; all other inputs classify `UNKNOWN`; only the last non-blank line controls the result | `audit-verdict-test.sh: 26 checks passed`, exit 0; also read `scripts/lib/audit-verdict.sh` source directly to confirm the strip/match logic matches the description | test run output above; source read | PASS | —
MC-016 | Read stdout evidence from the MC-004/005/010 live runs and the close-flow-test.sh assertions for the required markers; ran `git check-ignore -v .workflow/origin .workflow/audit-verdict .workflow/lock/pid`; grepped `from-issue.sh`/`change-workflow.sh`/`audit-verdict.sh` for token/credential handling | `Audit verdict: <class>` and close/skip/failure lines present; lock/origin refusal name the conflicting PID/issue; verdict artifact is exactly 3 tab-separated fields; origin is exactly `<repo>\t<issue>`; no credentials emitted; logs/cost.tsv format unchanged | All markers observed in transcripts above (`Closed .../ (verdict: ...)`, `Final audit verdict: NOT_READY — leaving ... open`, `Refusing to seed ...`, `another change-workflow.sh run (pid $$) holds this checkout`); `git check-ignore` reported all three paths ignored via `.gitignore:1`; grep found no token/password/secret literals — only unrelated `usage.*_tokens` (LLM token-count fields, not credentials) | commands above | PASS | —
MC-017 | Ran real `from-issue.sh --new` (unmodified script) in a scratch dir with a minimal `REQUIREMENTS.md` containing a `# Project brief` section, against real issue `unclehq/stagegate#2` | Same output/behavior as before the change: writes the brief section, prints the hint, exits, no `.workflow/` created, no GitHub write | Exit 0; `"Updated REQUIREMENTS.md project brief from issue unclehq/stagegate#2"` + `"Run: ./scripts/stagegate.sh"` printed (unchanged hint); `.workflow/` absent afterward; matches `CHANGE_TEST_REPORT.md`'s separately-executed pre/post-change byte-diff (also reported identical) | scratch-run transcript above | PASS | —
MC-018 | Ran `from-issue.sh` (no args, `--help`, `abc`) and `change-workflow.sh` (`--help`, `--version`, `bogus`) directly in this repo, then repeated `--help` for both from `/tmp` via absolute path and diffed | Matches BASELINE §6/§8/§9 exactly; CWD-independent | All outputs/exit codes matched described baseline (`from-issue.sh` no-arg/`--help` exit 0 stdout, `abc` exit 1 stdout w/ "Unrecognized issue argument"; `change-workflow.sh --help` exit 0, `--version` → `0.1.0` exit 0, `bogus` exit 1 stderr w/ "Unknown argument"); `/tmp` runs byte-identical to in-repo runs (`diff` produced no output) | command output above | PASS | —
MC-019 | Not executed — requires a full real agent-pipeline standalone run through internal gates (real `human_gate` decline, real approval-hash tamper-and-resume), which spends real budget and would risk this checkout's own live `.workflow/` state (currently `EXECUTE_CHECKLIST` for the real in-flight run). Structural-only review performed: read `human_gate()`/`verify_approval()` in `change-workflow.sh:246-324`, confirmed neither was modified by this change (no diff touches those functions; only `main`'s preamble and `FINAL_AUDIT` gained lines) | Decline halts further work; hash mismatch halts resume; reviewer-owned files only via reviewer call sites; state nodes unchanged `ANALYZE`…`COMPLETE`; standalone run exits 0, unaffected except one verdict line and a transient lock | Not observed via execution | source read only (`scripts/change-workflow.sh`) | NOT RUN | —
MC-020 | Hermetic `close-flow-test.sh` cases `seed-gate-complete-state-reseeds` and `seed-gate-fresh-checkout` (both in the 99 passing checks) cover the absent-state and `COMPLETE`-state scenarios; the "empty state" scenario is structurally identical to fresh-checkout in the seed-gate code path (`run_in_flight` is false for both) | Each scenario accepted as fresh/restartable; no prior in-progress state incorrectly reused | Both cases assert `SEED_WRITE` (i.e., not refused) | close-flow-test.sh pass output | PASS | —
MC-021 | Read `.gitignore` and the new-file list; confirmed via `git check-ignore -v` (MC-016) that `.workflow/origin`, `.workflow/lock/pid`, and the widened `.workflow/audit-verdict` are all gitignored; hermetic `preflight-standalone-unaffected` case (in the 99 passing checks) confirms the origin preflight is skipped with no `STAGEGATE_ORIGIN_*` set even with a foreign origin file present | Standalone execution unaffected by new metadata; issue-seeded runs create new metadata safely; ownerless legacy in-progress state refuses; existing formats not migrated | `preflight-standalone-unaffected` case passed (`expect_status 1`, `expect_not_out "Refusing to resume"` — i.e., it failed on its own unrelated validation, not the origin check); `.gitignore` confirmed unchanged format for `state`/`cost.tsv`/`CHANGE_REQUEST.md` | close-flow-test.sh pass output; `.gitignore` read | PASS | —
MC-022 | Read `README.md:114-` ("Start from a GitHub issue") and `scripts/README.md:63-113` (`from-issue.sh` section, state-file table) and compared every documented claim against the actual source and against the live transcripts captured in MC-002/003/004/005/007/010 | Docs no longer describe a print-and-stop handoff; accurately describe blocking prompt, auto-run, conditional close, skip/refusal messages, origin ownership, lock refusal, no bypass flag, unchanged `--new` | Every checked claim matched: exact confirmation word `RUN` (matches live prompt text), no `--yes`/bypass flag (grepped, none present), blocking on a pipe (matches MC-003), origin/lock refusal message wording matches `scripts/README.md:106-112` and the live MC-010 output, state-file table (`origin`, `audit-verdict`, `lock/pid`) matches the actual paths in both scripts | doc excerpts + grep above | PASS | —
MC-023 | Re-ran `for f in scripts/*.sh scripts/lib/*.sh scripts/tests/*.sh; do bash -n "$f"; done` (9 files), then `./scripts/tests/audit-verdict-test.sh`, then `./scripts/tests/close-flow-test.sh`, then the BASELINE §8 argument-contract commands (reused for MC-018) | All syntax checks and automated tests exit 0; coverage includes both satisfactory verdicts, negative branches, driver statuses 1/7/130, curl fallback, unauthenticated/missing `gh`, close failure, decline, lock contention, foreign origin; no real GitHub write | `bash -n`: 9/9 OK; `audit-verdict-test.sh: 26 checks passed`; `close-flow-test.sh: 99 checks passed`; BASELINE §8 replay matched (MC-018); confirmed no real `gh issue close` in any of the above (hermetic tests use a stubbed `gh`, and this session's own live exercises used stub/intercept `gh` as documented per-row above) | command output above | PASS | —
MC-024 | Not executed — the full check requires a disposable branch/checkout with a completed test run and a *real* disposable issue actually closed by the feature, then a revert-and-resmoke pass; no such live-closed issue exists (this pass performed no real closes, see MC-004/005). Partial evidence: `CHANGE_TEST_REPORT.md`'s "Rollback test" section (already executed, cited, not re-run) confirms the pre-change `from-issue.sh` (`git show HEAD:`) runs unmodified against this tree for the `--new` path | Reverted script returns to seed-and-hint; no auto-run/close; existing state formats need no migration; already-closed issues are not reopened by rollback | Not independently re-verified against a real closed issue | `CHANGE_TEST_REPORT.md` §"Rollback test" (cited, not re-executed) | NOT RUN | —
MC-025 | Sourced `scripts/from-issue.sh` directly with `STAGEGATE_FROM_ISSUE_SOURCE_ONLY=1` and confirmed `confirm_and_run_workflow` is defined with no CLI executed (exit 0, no fetch/prompt attempted); ran the full CLI with the variable unset and with it explicitly `=0` and diffed output | Unset/`0` run the normal CLI; `1` exits before parsing/fetch/prompt/close; sourcing with `1` defines the helper functions; no help/CLI-flag equivalent exists | Sourcing with `=1`: functions defined, no output, no CLI ran; unset vs `=0`: byte-identical usage output (`diff` empty); grepped `usage()` text — the hook is not mentioned | command output above | PASS | —
MC-026 | Not executed — requires two real disposable GitHub clones each driving a real live agent pipeline to completion against one shared issue, which is exactly the class of live, budget-spending, dual-write scenario this pass avoided by policy (see report header) | Each clone's lock/origin/run/hash binding stays independent; record whether both attempt `gh issue close` and how the second is reported | Not observed | — | NOT RUN | —
MC-027 | Not executed — `CHANGE_TEST_REPORT.md` already notes this scenario "cannot be provoked deterministically" (PID-reuse race) and is a documented, accepted residual risk (`UPDATED_CHANGE_PLAN.md` §22, "stale-lock false negative", explicitly "safe direction only ... not blocking"). No new attempt was made to force a real PID collision in this pass | First invocation refuses citing the live unrelated PID; second (after it exits) clears the stale lock and proceeds | Not observed | `CHANGE_TEST_REPORT.md` "Untested areas"; `UPDATED_CHANGE_PLAN.md` §22 (cited) | NOT RUN | —
MC-028 | Ran the same real-fetch / stubbed-driver / intercepted-`gh` harness as MC-004, with `change-workflow.sh` stub writing `FINAL_AUDIT.md`=`NOT READY` | Verdict artifact class `NOT_READY`; origin matches; issue remains `OPEN`; no close/comment; output reports the verdict and open disposition; exit 0 | Output: `"Final audit verdict: NOT_READY — leaving unclehq/stagegate#2 open."`; stub-`gh` close log not created (no close attempted); exit 0 | "MC-028/MC-006 mechanics" transcript above | NOT RUN (full check, live pipeline + real GitHub state confirmation) — close-decision logic for this exact verdict independently confirmed correct | —

## Acceptance-criteria summary

- AC-1 (seed→prompt→run): confirm/decline/piped-prompt mechanics verified live (MC-002, MC-003); full same-invocation driver streaming not verified live (MC-001 NOT RUN).
- AC-2 (decline runs nothing): verified live (MC-002) — PASS.
- AC-3 (READY/READY WITH NON-BLOCKING → closed+comment): close-decision logic and exact `gh issue close` arguments verified live against the real issue with the close call intercepted (MC-004, MC-005); no real GitHub write was performed or is claimed as verified end-to-end.
- AC-4 (NOT READY/incomplete → open): verified live for NOT_READY (MC-028) and for all negative branches hermetically (MC-006); driver-failure propagation verified live (MC-007).
- AC-5 (curl fallback → skip+message): verified hermetically (MC-008); real unauthenticated-`gh` variant not run (see MC-008 row).
- AC-6 (`--new` unchanged): verified live (MC-017) — PASS.
- AC-7 (`bash -n` + arg contract): verified live (MC-018, MC-023) — PASS.

## Preserved-behavior summary

B-08 (`--new`), B-09 (arg contract), CWD independence, and the new-vs-legacy `.workflow/` coexistence (MC-020, MC-021) all verified live or via the real-driver hermetic harness and PASS. I-01/I-02/I-03 (existing internal gates, approval hashing, reviewer-owned files) were **not exercised end-to-end** in this pass (MC-019 NOT RUN) — confirmed only by reading that `change-workflow.sh`'s diff does not touch `human_gate()` or `verify_approval()`.

## Changed-behavior summary

Origin binding, lock, fresh-audit binding, and the exact-match classifier (I-08, I-10, I-11) all have direct evidence: either live real-driver runs (MC-010, MC-012, MC-013, MC-014) or live real-fetch/real-close-argument runs (MC-004, MC-005, MC-028). The two branches this report cannot confirm end-to-end are (a) a real multi-stage agent pipeline organically producing a verdict, and (b) an actual `gh issue close` executing against a live issue — both are irreversible/costly live actions outside this verification pass's scope; see NOT RUN rows.

## Invariant summary

I-04 (CWD independence): PASS (MC-018). I-05 (GitHub mutation gated): PASS on every gate individually exercised (confirmation, origin, hash, verdict, `USED_GH`/auth) except the terminal live `gh issue close` call itself, which was intercepted rather than executed — see MC-004/005/028. I-06/I-08 (verdict decoupling / fail-closed close): PASS (MC-004, MC-005, MC-006, MC-028, MC-015). I-07 (human edit window, no TTY bypass): PASS (MC-002, MC-003). I-09 (gh missing/unauthenticated never silently no-ops or crashes): PASS, stubbed only (MC-008). I-10 (single-writer lock): PASS (MC-012, MC-013). I-11 (origin-provable resume): PASS (MC-010; `change-workflow.sh`-side mirror only hermetically, not independently re-run live).

## Regression summary

No regression found. `bash -n` clean on all 9 shell files; the documented six-script argument contract (`scripts/README.md:115-129`) is byte-identical to baseline for both scripts checked (MC-018); `--new` path byte-diff-equivalent behavior reconfirmed live (MC-017); no new warnings; no pre-existing failure changed.

## Unresolved defects

None found. Zero FAIL results this pass — no `DEFECTS.md` created. Known, previously-disclosed residual risks (not defects, not newly discovered): stale-lock PID reuse false-refusal (MC-027, accepted in `UPDATED_CHANGE_PLAN.md` §22), cross-clone concurrent-issue collision (MC-026, explicitly out of scope), and verdict-classifier dependence on the reviewer prompt's exact last-line phrasing (`UPDATED_CHANGE_PLAN.md` §22) — all previously identified, none contradicted by anything observed here.

## Recommendation

Do not declare this change complete yet. Of the 28 checklist items, 11 are NOT RUN or partially NOT RUN (MC-001, MC-004 full, MC-005 full, MC-008 real-`gh`-logout variant, MC-009 real variant, MC-010 driver-side mirror not independently re-run live, MC-011, MC-019, MC-024, MC-026, MC-027, MC-028 full) because their expected result requires either a real, budget-spending multi-stage agent-pipeline run to a genuine `COMPLETE`, or an actual irreversible `gh issue close` against a live GitHub issue — both of which this pass deliberately avoided to prevent colliding with this checkout's own real in-flight run (issue `unclehq/stagegate#2`, state `EXECUTE_CHECKLIST`) or mutating live GitHub state without separate authorization. Every check that could be executed safely (17 of 28, plus partial/logic-level confirmation on 3 more of the NOT RUN items) PASSED with no regressions and no defects. The remaining live checks should be run deliberately, on a dedicated disposable GitHub issue, with explicit authorization to spend agent-pipeline budget and to perform a real `gh issue close`, before declaring the change complete.
